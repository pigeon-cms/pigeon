#embed("header")
#embed("nav")
<h1>#(plural)</h1>

#if(count(items) == 0) {
<h2>No #lowercase(category.plural) found.</h2>
}

#for(item in items) {
<h2>#(items.content.first.value) #(items.date)</h2>
}

<a href="/content/#(category.plural)/create">Create a #lowercase(category.name)</a> <!-- TODO: singular name -->
