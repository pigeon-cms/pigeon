#embed("header")
#embed("nav")
<h1>Users</h1>
<section class="content">
<ul>
#for(user in users) {
	#if(user == currentUser) {
		<p>It's a me!</p>
	}
	<li>#(user.name)  #(user.email) #(user.timeZoneAbbreviation)</li>
}
</ul>
</section>
#embed("footer")
