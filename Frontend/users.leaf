<!doctype html>
<html lang="en">
    <head>
        <title>Pigeon CMS</title>
    </head>
    <body>
    	<nav>
            #embed("administration")
        </nav>
        <img src="/images/pigeon.svg" />
        <ul>
        #for(user in users) {
        	#if(user == currentUser) {
        		<p>It's a me!</p>
	        }
        	<li>#(user.name)  #(user.email)</li>
	    }
		</ul>
    </body>
</html>